apply plugin:"java"
apply plugin:"application"
apply plugin:"checkstyle"

mainClassName = "App"

task hello {
        println 'Hello world!'
}

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.2'
}
